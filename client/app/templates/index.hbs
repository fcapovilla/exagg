<div class="side-bar">
  <div class="navbar navbar-default">
    <div class="navbar-form" id="mainmenu">
      <div class="btn-group">
        <button class="btn btn-primary hidden-tiny-phone">
          <img id="spinner" class="invisible" src="img/ajax-loader.gif">
        </button>
        <button {{action "addFeed"}} class="btn btn-default" title="Abonnement">
          <i class="fa fa-plus"></i>
        </button>
        <a class="btn btn-default" href="/logout" title="Déconnexion">
          <i class="fa fa-sign-out"></i>
        </a>
      </div>
      <div class="btn-group mobile-item-buttons">
        <button class="btn btn-default mobile-item-button visible-xs invisible" id="mobilePrevItem" title="Suivant">
          <i class="fa fa-arrow-circle-o-up"></i>
        </button>
        <button class="btn btn-default mobile-item-button visible-xs invisible" id="mobileNextItem" title="Précédent">
          <i class="fa fa-arrow-circle-o-down"></i>
        </button>
      </div>
    </div>
  </div>
  <div class="dragbar hidden-xs"></div>
  <div class="feed-list">
    <ul class="list-unstyled" id="feed-list">
      <li>
        <div {{action "selectItems"}} class="folder-title {{if itemsSelected "active"}}">
          <div  class="folder-badge">
            <span class="badge badge-primary">1518</span>
          </div>

          <div  class="folder-text">
            <i class="folder-icon fa fa-asterisk"></i>
            Tous les items
          </div>
        </div>
      </li>
      <li>
        <div {{action "selectFavorites"}} class="folder-title {{if favoritesSelected "active"}}">
          <div class="folder-text">
            <i class="folder-icon fa fa-star"></i>
            Favoris
          </div>
        </div>
      </li>
      {{#each model as |folder|}}
        {{#folder-element model=folder onEdit=(action "editModel") onSelect=(action "selectModel") selectedElement=selectedElement}}
          {{#each folder.feeds as |feed|}}
            {{feed-element model=feed onEdit=(action "editModel") onSelect=(action "selectModel") selectedElement=selectedElement}}
          {{/each}}
        {{/folder-element}}
      {{/each}}
    </ul>
  </div>
</div>
<div class="content">
  {{outlet}}
</div>
