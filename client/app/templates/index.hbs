{{title "Exagg (" totalUnreadCount ")"}}

<div class="side-bar">
  <div class="navbar navbar-default">
    <div class="navbar-form" id="mainmenu">
      <div class="btn-group">
        <button class="btn btn-primary hidden-tiny-phone">
          <img id="spinner" class="invisible" src="img/ajax-loader.gif">
        </button>
        <button {{action "addFeed"}} class="btn btn-default" title="{{t "addFeed"}}">
          <i class="fa fa-plus"></i>
        </button>
        <a class="btn btn-default" href="/logout" title="{{t "logOut"}}">
          <i class="fa fa-sign-out"></i>
        </a>
      </div>
      <div class="btn-group mobile-item-buttons">
        <button class="btn btn-default mobile-item-button visible-xs invisible" id="mobilePrevItem" title="{{t "next"}}">
          <i class="fa fa-arrow-circle-o-up"></i>
        </button>
        <button class="btn btn-default mobile-item-button visible-xs invisible" id="mobileNextItem" title="{{t "previous"}}">
          <i class="fa fa-arrow-circle-o-down"></i>
        </button>
      </div>
    </div>
  </div>
  <div class="dragbar hidden-xs"></div>
  <div class="feed-list">
    <ul class="list-unstyled" id="feed-list">
      <li>
        <div {{action "selectModel" "items"}} class="folder-title {{if itemsSelected "active"}}">
          <div  class="folder-badge">
            <span class="badge badge-primary">{{totalUnreadCount}}</span>
          </div>

          <div  class="folder-text">
            <i class="folder-icon fa fa-asterisk"></i>
            {{t "allItems"}}
          </div>
        </div>
      </li>
      <li>
        <div {{action "selectModel" "favorites"}} class="folder-title {{if favoritesSelected "active"}}">
          <div class="folder-text">
            <i class="folder-icon fa fa-star"></i>
            {{t "favorites"}}
          </div>
        </div>
      </li>
      {{#each model as |folder|}}
        {{#folder-element model=folder onEdit=(action "editModel") onSelect=(action "selectModel") selectedElement=selectedElement}}
          {{#each folder.feeds as |feed|}}
            {{feed-element model=feed onEdit=(action "editModel") onSelect=(action "selectModel") selectedElement=selectedElement}}
          {{/each}}
        {{/folder-element}}
      {{/each}}
    </ul>
  </div>
</div>
<div class="content">
  {{outlet}}
</div>
